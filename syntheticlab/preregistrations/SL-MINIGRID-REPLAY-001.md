# Preregistration — SL-MINIGRID-REPLAY-001

For identical environment ID, seed, and action sequence:

require identical:
- sanitized observation sequence;
- rewards;
- terminated/truncated state.

Purpose:
validate deterministic replay checkpoints for the external environment adapter.
