# Preregistration — SL-RICH-REFLECTION-BRIDGE-001

Validate:
rich subject-visible evidence → structured reflection gateway → validated self-model proposal → audit trail.

Use deterministic fake provider only.

Reflection must not directly mutate action policy in this experiment.
