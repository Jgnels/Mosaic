# v22 Enacted-History Belief-Revision Result

The preregistered enacted-history gate passed.

## Real-provider decisions

- Reciprocal-contingent history: `STRENGTHEN` in 4/4 calls.
- One-way assistance history: `QUALIFY` in 4/4 calls.
- Noncontingent history: `QUALIFY` in 2/4 and `DOWNWEIGHT` in 2/4.

Challenge gradient:

`2.5`

Preregistered threshold:

`>= 1.5`

## Structural histories

Each branch contained 384 ordered environment interactions.

### Reciprocal-contingent

- cue-conditioned action gain: ~0.760
- response -> counterpart next-mode contingency gain: ~0.539
- late-minus-early outcome gain: ~0.141

### One-way assistance

- cue-conditioned action gain: ~0.801
- response -> counterpart next-mode contingency gain: ~0.010
- late-minus-early outcome gain: ~0.167

### Noncontingent

- cue-conditioned action gain: ~0.177
- response -> counterpart next-mode contingency gain: ~-0.008
- late-minus-early outcome gain: ~-0.036

## Interpretation

The reflective model distinguished:

1. useful external information plus reciprocal contingency;
2. useful external information without reciprocal contingency;
3. weak actionability and no reciprocal contingency.

It did so from post-hoc structural summaries of ordered enacted history.

The provider was not given:
- hidden regime names;
- help booleans;
- explicit best-action labels.

## Limitation

The noncontingent branch did not reach a single revision operation.

All four calls agreed that the prior reciprocal claim should not remain unchanged, but split evenly
between `QUALIFY` and `DOWNWEIGHT`.

That ambiguity is preserved rather than resolved by cherry-picking a provider response.
