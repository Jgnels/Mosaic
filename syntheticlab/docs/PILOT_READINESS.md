# Pilot Readiness

## Pilot R — Real reflection

Status:
**PREFLIGHT READY; REAL CLOUD CALL NOT YET EXECUTED IN RELEASE BUILD**

Validated offline:
- provider request/response contract;
- Interactions response parsing;
- hidden thought-step exclusion;
- strict structured proposal validation;
- restricted ontology injection rejection;
- reflection-call budget;
- eligibility gate;
- exact pre-fork snapshot;
- observational-only reflection isolation;
- action-policy equality against no-reflection control.

Remaining execution dependency:
- a local Gemini API credential in an environment variable.

## Pilot E — External MiniGrid

Status:
**EXECUTED**

Validated against real MiniGrid:
- adapter creation;
- partial observation sanitization;
- opaque mission handling;
- three real environments;
- seeded deterministic replay;
- paired Adaptive vs Random benchmark.

## Interpretation

The external pilot has produced first real environment data.

The reflection pilot has reached the boundary where another offline simulation adds little value. The next missing datum is the actual structured output of the first real provider call.
