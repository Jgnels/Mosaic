# v27 Semantic Measurement Reliability Result

The semantic measurement instrument was highly repeatable.

Mean within-item evaluator distance:

`0.0133488529`

Seven of nine propositions received identical semantic vectors across all three evaluator passes.
The remaining two showed only small score variation.

The convergence classification was stable across all three independent evaluator replicates:

- A: `SEMANTIC_PARTIAL_CONVERGENCE`, ratio `0.85235`
- B: `SEMANTIC_PARTIAL_CONVERGENCE`, ratio `0.85235`
- C: `SEMANTIC_PARTIAL_CONVERGENCE`, ratio `0.80078`

Aggregated result:

`SEMANTIC_PARTIAL_CONVERGENCE`

Aggregated initial semantic distance:

`0.5026279754`

Aggregated final between-branch distance:

`0.4191726690`

Ratio:

`0.8339620745`

## Key unresolved issue

Mean final within-branch trajectory dispersion:

`0.5304478266`

Between-branch centroid distance:

`0.4191726690`

Branch signal / trajectory variance ratio:

`0.7902241238`

Therefore:

`BRANCH_SIGNAL_NOT_SEPARATED_FROM_TRAJECTORY_VARIANCE`

## Interpretation

The v26 semantic convergence result is measurement-stable.

However, the remaining differences between branch centroids are smaller than the stochastic
variation among two shadow trajectories started from the same branch.

The correct claim is:

> repeated shared experience produced robustly measured partial semantic convergence, but the
> residual branch-history signal is not yet cleanly separated from provider-level trajectory
> stochasticity.
