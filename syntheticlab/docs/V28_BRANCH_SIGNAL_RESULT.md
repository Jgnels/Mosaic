# v28 Branch-History Signal Replication Result

Six shadow trajectories per branch completed the same four later shared-experience epochs.

Final semantic sample:

`18 trajectories`

## Convergence

Initial between-branch semantic distance:

`0.5026279754`

Final:

`0.2417061946`

Final / initial:

`0.4808848820`

Classification:

`SEMANTIC_CONVERGENCE`

## Residual branch-history signal

Mean within-branch trajectory dispersion:

`0.4392715161`

Between-branch centroid distance:

`0.2417061946`

Signal / trajectory variance:

`0.5502432681`

Permutation pseudo-F:

`1.6360052562`

20,000-permutation p-value:

`0.1378431078`

Status:

`BRANCH_HISTORY_SIGNAL_UNRESOLVED`

## Interpretation

Under the current SelfModel-only reflection architecture, repeated sufficiently similar later
experience substantially converged the semantic beliefs of branches created by different enacted
histories.

The remaining grouping by historical branch was not detectably stronger than ordinary
provider-level trajectory variation.

This does not erase the earlier causal history result:

- divergent enacted histories created different canonical SelfModels;
- those SelfModels initially caused different interpretations of identical evidence.

It establishes a later developmental result:

> when historical influence is carried primarily through the active canonical hypothesis, repeated
> common later evidence can substantially wash out the earlier semantic separation.

The canonical branch states themselves were not changed by the shadow experiment.
