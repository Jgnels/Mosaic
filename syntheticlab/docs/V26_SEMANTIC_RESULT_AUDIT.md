# v26 Semantic Convergence Result Audit

The blinded fixed-ontology semantic audit classified the four-shared-epoch result as:

`SEMANTIC_PARTIAL_CONVERGENCE`

Initial mean pairwise semantic distance:

`0.4953297967`

Final mean pairwise semantic distance:

`0.4221951794`

Final / initial:

`0.8523516700`

This resolves the v25 lexical measurement conflict in the direction of partial semantic convergence.

## New reliability concern

The two final shadow trajectories within the same branch remained substantially different:

- noncontingent branch: `0.6123724357`
- one-way branch: `0.5830951895`
- reciprocal branch: `0.4082482905`

Mean within-branch final trajectory dispersion:

approximately `0.5346`.

That exceeds the final between-branch centroid distance:

`0.4222`.

Therefore the current experiment does not cleanly separate:

- persistent branch-history signal;
from
- stochastic shadow-trajectory variation.

## Scientific status

Supported:

> Shared later experience produced partial convergence in the blinded semantic metric.

Not yet supported:

> The remaining semantic differences are robustly attributable to branch history rather than
> trajectory-level stochasticity.

v27 measures evaluator repeatability and branch-signal separation explicitly.
