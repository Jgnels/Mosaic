# Next Creative Decision Gate

The project has reached a genuine intervention boundary.

## What is now available

- persistent third-person longitudinal SelfModel;
- learned nonlinguistic Causal PerspectiveAnchor;
- deterministic deictic binding;
- FunctionalSelfIndex;
- shadow metacognition with zero enacted influence.

## Current maximum automatic level

`SHADOW_ONLY`

## Decision requiring human research-lead approval

Should Dagmay advance to:

`RETRIEVAL_ATTENTION_ONLY`

in an exact-fork experiment?

This would be the first time the functional self-index is allowed to causally influence
later cognition.

It would still not directly influence environment actions.

## Technical recommendation

Approve the bounded retrieval/attention experiment before any action-policy feedback.

Reason:

It creates a clean causal ladder:

```text
self-index exists
    ↓
self-index changes attention/retrieval
    ↓
observe whether cognition changes
    ↓
only later consider bounded action influence
```

Jumping directly to action feedback would make it much harder to determine what the
self-model itself contributed.

## Required human response

Approve or decline the `RETRIEVAL_ATTENTION_ONLY` exact-fork intervention.

No further automatic escalation will occur without that decision.
