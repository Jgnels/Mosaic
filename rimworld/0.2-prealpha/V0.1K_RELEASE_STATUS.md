# Dagmay 0.1K Release Status

## Designed

PASS.

RimWorld 0.1 cognitive scope remains frozen at the 0.1J.3 baseline.

## Implemented

PASS.

The engineering audit repaired the RimWorld compile blocker and hardened certification format v2 against unrelated-store, mismatched-counterpart, secondary-subject, duplicate-memory, unhealthy-store, and non-atomic-sidecar false evidence. The 0.1L persistence-torture and 0.1M failure-isolation harnesses are implemented without changing cognition or live provider behavior.

## Statically verified

PASS on Windows.

`python tools\static_verify.py` checked 74 C# files. All PowerShell files parse, and a synthetic active-store fixture proved the checker rejects an unrelated historical PASS.

## Compiled

PASS on the owner's Windows machine against local RimWorld 1.6.4871 assemblies.

Core, Providers, Tests, IntegrationHarness, and `Dagmay.RimWorld` compiled with zero warnings and zero errors in the full development loop.

## Tested in isolation

PASS.

- 41 contract tests passed.
- All six integration scenarios passed (603 assertions total).
- `PersistenceTorture` passed 238 assertions across six simulated save/reload cycles.
- `FailureIsolation` passed 103 assertions across provider, validation, interrupted-commit, mismatch, and pending-shutdown failures.
- SyntheticLab v20's zero-cloud integrated suite and v21's scripted lived-evidence checkpoint/resume test passed separately; their invariant gaps remain reported and unmodified.

## Tested in RimWorld

PASS for the copied-save social-path certification scenario.

Certification format v2 returned `Status=PASS` and `Gate.Overall=True` for the active StoreId after a real RimWorld save/reload. It recorded three social events, exact stable-counterpart provenance and relationship-sensitive memory for all three, two reflection-eligible direct-relationship events, and healthy identity, experience, and reflection stores. The combined log contained zero Dagmay failure lines.

RimWorld developer mode was used to make romance possible in the all-women test colony. That changed world setup but did not bypass a Dagmay gate; the certification still required observed RimWorld relationship/opinion changes and verified their complete persisted Dagmay path.

## Evidence

The combined active-store report is retained at `artifacts\Dagmay-diagnostics-latest.txt` in the audited source tree.