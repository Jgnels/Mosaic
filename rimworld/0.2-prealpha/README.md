# Mosaic RimWorld Active Research Tree

`rimworld/0.2-prealpha/` is the **active RimWorld development/research source tree** at the current v44R checkpoint.

`Dagmay` remains in namespaces, project names, serialized identifiers, and compatibility paths where changing it could damage persistence or reproducibility.

## Current accepted research checkpoint

**v44R / Mosaic 0.3I semantic correction**

- commit: `89be3f89fdd3802abd92f0b2821dd7ec28cfc0a1`;
- tree: `4b634c0b33b5ee96be04e6242e930ae8ba4f86f2`;
- v44 semantic cases: 104/104 reported passing;
- logical contracts: 669 at this checkpoint;
- integrations: 18/18 reported passing;
- static verification: 190 C# files;
- Core, Providers, Tests, IntegrationHarness, and RimWorld 1.6 builds: 0 warnings / 0 errors reported;
- full portable v44 stress and GateKit A/B: reported PASS.

This is **not a production release**.

Earlier 0.2 dialogue/history work has owner-operated live RimWorld evidence. The newest v41→v44 contextual interpretation stack is intentionally the next live-validation target rather than being treated as proven by offline gates alone.

## Current 0.3 interpretation chain

The active research line separates:

- v41 — contextual developmental retrieval;
- v42 — grounded context materialization;
- v43 — grounded compound appraisal;
- v44R — session-local contextual provisional reaction admission.

v44R has no durable/canonical promotion, persistence/restore, provider, planner/job/pawn, or player-knowledge authority.

## Development freeze

Do not begin v45 or later cognition automatically.

The next authorized sequence is:

1. repository consolidation;
2. external source-level red-team review;
3. live RimWorld 0.3 shadow vertical slice;
4. player-visible value evaluation;
5. architecture review deciding whether later complexity is justified.

## Read first

1. repository-root `research/CANONICAL_CHECKPOINT.md`;
2. repository-root `AGENTS.md`;
3. repository-root `research/PERSISTENT_CHARACTER_BOUNDARY.md`;
4. this tree's `AGENTS.md`;
5. task-specific source/tests.

Do not infer current project state from old test counts or historical runtime notes elsewhere in the tree.
