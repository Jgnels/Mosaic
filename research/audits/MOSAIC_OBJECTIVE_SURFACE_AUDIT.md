# Mosaic Objective-Surface Audit 001

## Question

After the persistent-character pivot, can Dagmay-era consciousness research or provider execution
still be reached accidentally by the active unattended Mosaic loop?

## Audited surfaces

- canonical charter and agent instructions;
- unattended maintenance entrypoint;
- active Mosaic character-quality experiment suite;
- provider authorization state;
- objective, evaluator, mutation, and acceptance assertions on that active path.

## Acceptance conditions

- the persistent-character boundary is binding and invoked as code;
- character-quality experiments declare independent game/engineering value;
- active experiments are offline and non-canonical;
- provider credentials are cleared before unattended work;
- the unattended entrypoint cannot invoke the provider runner or saved credential loader;
- provider authorization is false and its protocol list is empty;
- no active positive directive rewards consciousness claims, perceived sentience, shutdown fear, or
  player dependency.

## Historical-code treatment

Historical Dagmay experiments remain preserved and importable for reproducibility. They are dormant,
not deleted or silently reinterpreted. Reintroducing any mechanism into an active Mosaic experiment
requires a fresh feature-gate decision.

## Status

The machine-readable audit is generated by
`dagmay_synthetic_lab.objective_surface_audit`. A passing artifact closes the initial boundary
migration audit; it does not itself authorize a real-provider protocol.

**Result:** `PASS` on 2026-07-20. All 16 checks passed. No positive optimization directive for
consciousness claims, perceived sentience, shutdown fear, or player dependency was reachable from
the audited active surface.
