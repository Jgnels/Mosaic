# Dagmay Canonical Handoff

Read `AGENTS.md`, then `research/CURRENT_STATE.md`.

The essential state:

- RimWorld track is owner-reported at 0.1RC; reconcile exact build/commit from actual code repo.
- RimWorld 0.1 cognitive scope is frozen for reliability/closure.
- SyntheticLab's latest completed real-provider experiment is v31.
- v31's attribution-framing headline claim is invalidated.
- No new real-provider SyntheticLab experiment should run until provider-facing payload leakage and
  provenance controls are repaired and tested.
- The 300-source research synthesis and full 30-resource audit matrix are preserved in this export.
- Full original Atlas/audit ZIPs should also be archived using the hashes in
  `research/SOURCE_ARTIFACT_MANIFEST.md`.
- The dedicated RTX 3090 desktop is the intended canonical lab host.
- GitHub should be the durable source of truth; new chats should bootstrap from repository files,
  not giant transcript dumps.

Project invariant:

> An individual is defined by the continuity of its identity through time, not by the specific
> language model that generates its thoughts.

Research principle:

> We should not design Dagmay to perform personhood. We should create controlled conditions under
> which person-like properties might or might not emerge, then measure honestly.
